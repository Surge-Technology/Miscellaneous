package BasicsProto;

public class WhileLoopProto {
	public static void main(String args[])
    {
        int x = 1;
        String sampleStr = "Value of x::";
        // Exit when x becomes greater than 4
          while (x <= 4)
        {
             System.out.println(sampleStr + x);
                  //increment the value of x for next iteration
                  x++;
              }
     }
}
